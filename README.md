<p align="center">
  <img src="images/Kotlin-java.png"/>
</p>

# 🏨 Hotel Booking System

👋 Привет!

Этот проект — решение тестового задания на позицию **стажёра Kotlin/Java разработчика**.  
В рамках задания реализована **простая система бронирования номеров в отеле** на базе **Spring Boot** и **PostgreSQL** без использования ORM.

Проект выполнен на **Java 21** (допускается и Kotlin, но в данном решении выбран Java).

---

## 📌 Общая идея решения

Система оперирует тремя основными сущностями:

- **Гость (Guest)**
- **Номер (Room)**
- **Бронь (Booking)**

Архитектура приложения классическая для Spring Boot:

Будут запущены:

- **PostgreSQL**
- **Kafka (если требуется для доп. заданий)**

⚠️ Без PostgreSQL приложение не запустится.

## Запуск приложения
```bash
./gradlew bootRun
```

После запуска приложение будет доступно по адресу:
http://localhost:8080

Swagger UI:
http://localhost:8080/swagger-ui.html

## 📦 API

👤 Гости (GuestController)
- **POST /api/guests — создание гостя**
- **PUT /api/guests/{id} — редактирование гостя**
- **GET /api/guests — получить всех гостей (с пагинацией)**

🏨 Номера (RoomController)
- **POST /api/rooms — создание номера**
- **PUT /api/rooms/{id} — редактирование номера**
- **DELETE /api/rooms/{id} — удаление номера**
- **GET /api/rooms — получить все номера (с пагинацией)**
- **GET /api/bookings/free — поиск свободных номеров на дату** (Решил написать этот функционал к комнатам, т.к. с бронирование у нас не проверяется, а проверяется сама комната, доступна она или нет)


📅 Брони (BookingController)
- **GET /api/bookings/free — поиск свободных номеров на дату**
- **POST /api/bookings — создание брони**
- **PUT /api/bookings/{id} — редактирование брони**
- **POST /api/bookings/{id}/cancel — отмена брони**
- **GET /api/bookings — получить все брони (с пагинацией)**

## 🗄 Модель данных

Гость (Guest)
- **id — UUID** (безопаснее)
- **lastName — String**
- **firstName — String**
- **middleName — String**
- **dateOfBirth — LocalDate**
- **phone — String**

Номер (Room)
- **id — UUID**
- **floor — Integer**
- **number — String**
- **numberOfGuests — Integer**

Бронь (Booking)
- **id — UUID**
- **guestId — UUID**
- **roomId — UUID**
- **checkInDate — LocalDate**
- **checkOutDate — LocalDate**
- **status (ACTIVE, CANCELLED) — String** (можно сделать и через Enum, тогда надо сделать и "create enum" в БД. Я решил так не делать, так как пользователям нельзя самим ставим статус и при добавлении новых статусов надо было бы постоянно обновлять статус и изменять БД).

String хранится в БД как varchar
LocalDate хранится в БД как date


## 🧠 Бизнес-логика
**Поиск свободных номеров**
Номер считается свободным, если:
- нет активной брони
- даты бронирования пересекаются с искомым диапазоном

Используется SQL с 'NOT EXISTS', что:
- эффективно
- не требует загрузки лишних данных
- корректно обрабатывает пересечения дат

## 🧪 Тестирование
**Unit-тесты**
- Сервисы тестируются изолированно
- Используются моки DAO

Интеграционные тесты
- Реализованы с помощью Testcontainers
- Поднимается реальный PostgreSQL 
- Проверяется цепочка:
```nginx
Controller → Service → DAO → Database
```


🧹 Линтер

Проект отформатирован и соответствует стандартам Java-кода.
(При необходимости легко подключается Checkstyle или Spotless.)

📣 Kafka (опционально)

Реализована отправка событий в Kafka:

Создание / изменение гостя

Создание / изменение / отмена брони

Создание / изменение / удаление номера

Kafka-сервисы находятся в docker-compose.yml
(по умолчанию закомментированы).


## ⏱ Затраченное время
Примерная оценка:

Основной функционал: 10–12 часов

Тесты и доработки: 4–6 часов

Документация: 2 часа

## 🔀 Git workflow
Забыл изначально создать отдельную ветку для оразработки, из-за чего не пришлось делать PR.

✅ Итог

В рамках задания реализовано:

✔ DAO / Service / Controller слои
✔ Нативный SQL + Liquibase
✔ Swagger-документация
✔ Unit и интеграционные тесты
✔ Пагинация
✔ Kafka (опционально)
✔ Чистая архитектура

Спасибо за внимание! 🚀
Буду рад обратной связи 🙂